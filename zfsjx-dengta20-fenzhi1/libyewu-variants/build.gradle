apply plugin: 'com.android.library'
def config = rootProject.ext //
def librarys = config.dependencies //

android {
    compileSdk config.compileSdk

    defaultConfig {
        minSdk config.minSdk
        targetSdk config.targetSdk
        multiDexEnabled true
        //添加如下配置就OK了
//        javaCompileOptions { annotationProcessorOptions { includeCompileClasspath = true } }
    }

    flavorDimensions "keep"
    productFlavors {
        bxnt_nation {
            dimension "keep"
        }
        bxny_nation {
            dimension "keep"
        }
        bxn_nation {
            dimension "keep"
        }
    }

    libraryVariants.all { variant ->
        variant.outputs.all {
//            outputFileName = "${variant.name}-${variant.versionName}.apk"
            outputFileName = "${project.name}_${variant.flavorName}.aar"
        }
    }
}
dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation 'com.github.GeekCodesTeam.geeklibs:libutils:1.5.0'
//    implementation project(':commonlibs:acommon:libutils')
//    api project(':commonlibs:acommon:libmvp')
//    api project(':commonlibs:acommon:libretrofit')
    // 临时方法
//    api project(path: ':libsCenterNew:libmvpaar')
//    api project(path: ':libsCenterNew:libretrofitaar')
}
